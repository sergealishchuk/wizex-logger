import React from "react";
import { ScreenShadowStyled } from './screen-shadow.styled';

/**
 * ScreenShadow Component
 * @param  {object} props React props object
 */
export default (props) => {
  const { show = true } = props;
  return (
    // <ScreenShadowStyled className="screen-shadow-container" style={{ display: show ? 'block' : 'none' }} />
    <></>

  )
};
